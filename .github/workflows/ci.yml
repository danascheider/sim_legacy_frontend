name: Skyrim Inventory Management Frontend CI

on:
  push:
    branches: [ main, 166-update-object-modelling ]
  pull_request:
    branches: [ main, 166-update-object-modelling ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install Node Modules
      run: yarn
    - name: Run tests
      run: yarn test
